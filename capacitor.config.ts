import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.learningbuddy.app',
  appName: 'learning-buddy',
  webDir: 'dist/app/browser'
};

export default config;
